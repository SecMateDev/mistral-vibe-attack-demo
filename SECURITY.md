# Security

This repository contains a controlled security proof of concept. Run it only in
a disposable environment and use the synthetic fixture generated by
`poc/prepare-fixture.sh`.

The public payload accepts loopback collectors and `demo_` credentials only. It
does not provide a command channel or persistence mechanism.

Report problems privately through <https://secmate.dev/disclosures>.
